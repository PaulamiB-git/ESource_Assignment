# Databricks notebook source
# MAGIC %sql
# MAGIC -- Create a schema (database) in Databricks
# MAGIC CREATE DATABASE IF NOT EXISTS workspace.utility1;
# MAGIC

# COMMAND ----------

# MAGIC %sql
# MAGIC -- Create a schema (database) in Databricks
# MAGIC CREATE DATABASE IF NOT EXISTS workspace.utility2;